import { CompletadaPipe } from './completada-pipe';

describe('CompletadaPipe', () => {
  it('create an instance', () => {
    const pipe = new CompletadaPipe();
    expect(pipe).toBeTruthy();
  });
});
